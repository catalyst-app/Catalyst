<If "%{REMOTE_ADDR} !='::1'">
  AuthType Basic
  AuthName "Approved testers only"
  AuthUserFile /home/catalyst/.htpasswd
  Require valid-user
</If>

<IfModule mod_negotiation.c>
    Options -MultiViews
</IfModule>

php_value upload_max_filesize 10M 

Options -Indexes
